<Window x:Class="clientSocket.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:clientSocket"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Client App" Height="350" Width="525" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <TextBlock x:Name="block" Grid.Row="0" TextWrapping="Wrap" Background="#FFA0D4FF"/>
        </ScrollViewer>
        <TextBox x:Name="box" Text="Message..." Grid.Row="1"/>
        <Button x:Name="Btn" Content="Send Message" Grid.Row="2" Click="Btn_Click"/>
    </Grid>
</Window>
